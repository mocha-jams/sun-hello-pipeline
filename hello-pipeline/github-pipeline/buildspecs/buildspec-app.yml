version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
  build:
    commands:
      - echo "Building sample Lambda app"
      - zip -r app.zip index.js
artifacts:
  files:
    - app.zip
